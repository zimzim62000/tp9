{% extends 'base.html.twig' %}


{% block body %}

    <h2>add userCard</h2>

    {{ form_start(form, {'attr':{'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

<a href="{{ path('card_index') }}">back to list</a></li>
{% endblock %}
