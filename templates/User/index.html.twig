{% extends 'base.html.twig' %}

{% import "macro.html.twig" as macro %}

{% block body %}

    <h2>index user</h2>
    <ul>
        {% for userCard in userCards %}
            <li>
                {% if is_granted('ROLE_ADMIN') == true %}
                    <h6>{{ userCard.user.email }}</h6>
                {% endif %}
                {{ macro.usercard(userCard) }}
            </li>
        {% endfor %}
    </ul>

{% endblock %}
