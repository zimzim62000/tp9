{% extends 'base.html.twig' %}


{% block body %}

    <h2>index skin</h2>

    <ul>
        {% for skin in skins %}
            <li>
                {{ skin.name }}
                {{ skin.price }}
                {{ skin.user }}
                {{ skin.user }}
                <a href="{{ path('app_weaponskin_show',{ 'id' : skin.id})}}">Show</a>
            </li>
        {% endfor %}
    </ul>

{% endblock %}
