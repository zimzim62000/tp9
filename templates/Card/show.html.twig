{% extends 'base.html.twig' %}


{% block body %}

<h2>show Card</h2>

<ul>
    <li>
        {{ card.name }} <a href="{{ path('add_usercard', {"id" : card.id}) }}">Ajouter à la liste</a>
    </li>

</ul>

    <a href="{{ path('card_index') }}">back to list</a>
{% endblock %}
